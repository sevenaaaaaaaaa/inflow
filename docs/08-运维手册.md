# Insight Flow 运维手册（v1.0 / R2）

> 面向私有化部署客户的运维与排障指引。目标：**备份恢复 < 10 分钟、常见故障自助排障**。

---

## 1. 日常备份与恢复

### 1.1 备份机制

| 项 | 说明 |
|---|------|
| 自动备份 | 每日 09:15 UTC 随服务调度执行（`backup.daily`） |
| 备份内容 | `insflow.db`（在线 checkpoint，WAL 安全）+ `reports/` + `events/` |
| 备份位置 | `data-backup/YYYYMMDD-HHMM/` |
| 保留策略 | 30 天，超期自动清理 |
| 手动备份 | `insflow backup` |

### 1.2 恢复流程（SLA < 10 分钟）

```bash
# 1) 列出可用备份
ls data-backup/

# 2) 恢复（危险操作，自动把当前 data/ 存为 pre-restore 快照）
insflow backup --restore-from data-backup/20260916-1434 --yes

# 3. 重启服务
systemctl restart insflow   # 或 pkill + 重新启动
```

**回退保险**：恢复动作会先把当前 `data/` 移到 `data-backup/pre-restore-*`，恢复错了可以再恢复回来。

### 1.3 验证恢复成功

```bash
curl http://127.0.0.1:8400/health   # {"status":"ok","version":"1.0.0"}
insflow workspace list              # 数据可见即恢复成功
```

---

## 2. 排障流程（按 `insflow doctor` 输出自查）

### 2.1 第一步总是体检

```bash
insflow doctor -w <workspace-id>
```

五项检查（环境/凭据/插件/调度/磁盘）会给出 ❌/⚠️ 与**可执行修复建议**。常见问题对照：

| 体检输出 | 根因 | 处理 |
|---------|------|------|
| ❌ 主密钥未设置 | 环境变量缺失 | 设置 `INSFLOW_MASTER_KEY` 后重启（**注意**：换了密钥旧凭据不可解密，需重新接入） |
| ⚠️ 凭据保险库：无任何凭据 | 第一方数据未接入 | 访问 `/console/onboarding` |
| ⚠️ GSC/GA4 授权即将过期 | 90 天周期将到 | 等待自动轮换；若收到轮换失败告警 → 重新走授权向导 |
| ⚠️ 调度任务 0 个 | 建了服务但没建监控 / 显式关闭 | 控制台「监控任务」页创建，或检查 `INSFLOW_DISABLE_SCHEDULER` |
| ⚠️ 事件流有失败事件 | 数据源限流/网络 | 查 `insflow insight list`；GDELT 429 → 错峰 cron |
| ⚠️ 备份：从未备份 | 未挂 daily timer | 确认服务为 systemd/调度启动模式（不是裸进程） |

### 1.2 事件流回放（定位"什么时候开始坏的"）

```bash
# 最近 100 条事件
tail -n 100 data/events/<ws>/events.jsonl | python3 -m json.tool

# 关注的事件类型
# monitor.alert        → 采集失败（含错误信息）
# action.failed/dead   → 动作失败（dead = 重试耗尽）
# source.token_refresh_failed → 授权轮换失败（critical）
# auth.token_expiring  → 授权 7 天内到期预警
```

---

## 3. 告警分级与响应

| 级别 | 事件 | 含义 | 响应时限 |
|-----|------|------|---------|
| CRITICAL | `quota.exceeded` | 数据源配额熔断 | 当日：确认费用口径，必要时升降套餐 |
| CRITICAL | `source.token_refresh_failed` | 授权轮换失败（客户端授权已断） | **当天**重新授权（否则监控全部停摆） |
| HIGH | `monitor.error` | 监控任务连续失败 | 24h 内排查 |
| HIGH | `action.dead` | 动作重试 3 次耗尽 | 检查目标系统（OpenFlow/MFlow）可达性 |
| MEDIUM | `action.failed` / `quality_gate_blocked` | 单次失败 | 汇总后周处理 |

告警自动出站到飞书（`INSFLOW_ALERT_WEBHOOK_URL`）；每日摘要固定推送（`INSFLOW_DAILY_WEBHOOK_URL`）。

---

## 4. 升级与回滚

```bash
./deploy/upgrade.sh v1.0.1   # 自动备份 data/ → 拉版本 → 重启 → 健康检查失败自动回滚
```

升级后必做：`insflow doctor` 确认全绿。

## 5. 数据导出（防锁定承诺）

```bash
insflow export -w <ws> -o backup/ -f json   # 全量 JSON / CSV / MD
```

---

## 入口矩阵（控制台 / API / CLI）——工程维护产出（2026-09-18）

排查方式：静态扫描「API 端点、控制台路由、CLI 命令、模板引用」四者的交叉覆盖，
把"已开发但只有 API/CLI"的能力补上页面入口，其余明确标注为机器接口。

| 能力 | 控制台入口 | API | CLI |
|---|---|---|---|
| 9 个驾驶舱 | `/console/cockpit/*`（侧栏） | `/api/v1/cockpit/*` | — |
| 即席探索（9 种图 / 透视 / SQL / 叙事 / 口径） | `/console/explore` | `/api/v1/explore/*`、`/api/v1/semantics/*`、`/api/v1/narrative` | `insflow sql / narrate` |
| 告警规则 + 通知策略（静默/节流/升级） | **`/console/alerts`（新增）** | `/api/v1/alerts/*`、`/api/v1/notify/policy` | `insflow alerts check` |
| 成员与权限（角色/白名单/表达式 RLS/SCIM 说明） | **`/console/members`（新增）** | `/api/v1/members/role`、`/api/v1/rls/{allow,policies}` | — |
| 数据资产（GeoJSON 数据集/快照/导出/审计） | **`/console/assets`（新增）** | `/api/v1/geo/datasets`、`/api/v1/snapshots*` | `insflow snapshot` |
| 成熟度评估（问卷 → 五维雷达） | **`/console/maturity`（新增）** | `/api/v1/maturity*` | — |
| 问数（NL → 规则/LLM） | **🔎 顶部按钮 / ⌘K（新增）** | `/api/v1/agent/ask` | `insflow agent ask` |
| 竞品流量估算指数 | 竞品驾驶舱「流量估算指数」 | `/api/v1/estimate` | `insflow estimate` |
| 数据质量 SLA + 断点续采 | 运维舱面板 | `/api/v1/data-quality*` | `insflow dq check|backfill` |
| 归因与动作增量 | 行动验证舱面板 | `/api/v1/attribution/*` | `insflow attribution|lift` |
| 审计轨迹 / 导出 | `/console/audit`（侧栏） | `/api/v1/audit/admin?format=csv` | — |
| 快照归档 | `/console/snapshots` | `/api/v1/snapshots` | `insflow snapshot` |
| 隐私（DSAR/RTBF/留存） | —（合规操作，故意只给 API/CLI，避免误点） | `/api/v1/privacy/*` | `insflow privacy *` |
| 成员同步（SCIM 2.0） | 成员页有配置说明 | `/scim/v2/*`（Bearer，仅 IdP 调用） | — |
| 第一方推送 / MCP / API Key | —（机器接口） | `/api/v1/ingest`、`/api/v1/mcp/*`、`/api/v1/auth/keys` | — |
| 行业模板包 | — | `/api/v1/templates*` | `insflow template list|apply|export|validate` |
| 旅程框架（CJ-1/CJ-2 阶段定义 + 触点覆盖） | 旅程舱（阶段数据） | `/api/v1/journey/framework`、`/api/v1/journey/coverage` | `insflow journey` |
| Agent Skill 市场（技能包安装 → 热加载） | — | `/api/v1/skills/market*` | `insflow skill list|install` |
| 控制台静态资源（可缓存） | 自动引用 | `/console/static/app.{js,css}`（ETag/304，免登录） | — |
| 系统集成状态（通道健康/最近事件/死信） | `/console/integrations`（生态区） | `/api/v1/integrations/status` | — |
| 出站死信重放 | 集成页按钮 | `/api/v1/actions/dead-letters[/replay]` | `insflow action dead-letters|replay` |
| 自进化（提案/门禁/生效/回滚/配方） | `/console/evolution`（生态区） | `/api/v1/evolution/*`、`/api/v1/verification/results`、`/api/v1/playbooks*` | `insflow evolution list|propose|apply|rollback`、`insflow playbook --mine` |
| 备份 / 恢复演练 / 升级 | —（运维脚本） | — | `insflow backup`、`deploy/*.sh` |
