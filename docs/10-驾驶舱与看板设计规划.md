# Insight Flow 驾驶舱与看板设计规划

> v1.0（2026-09-16）。目标：把"Markdown 报告 + 表格"升级为**可视化驾驶舱**，让超级个体一屏看懂全域数据。
> 承接 docs/09 战略定位（全域监测 · 超级个体）。

---

## 1. 设计原则

| 原则 | 说明 |
|---|---|
| 零构建链 | 不引入 webpack/Vite。图表用**服务端渲染 SVG**（Python 生成，内联 HTML），少量原生 JS 只做交互（tooltip/切换） |
| 自包含可导出 | SVG 内联 → 报告可直接打印成 PDF、可复制、可脱敏白标，不依赖外部 CDN |
| 明暗双主题 | SVG 用 `currentColor` + CSS 变量，跟随控制台主题 |
| 信息层级 | 驾驶舱（Cockpit）→ 看板（Panel）→ 图表（Chart）三级；一屏一主题 |
| 图表即证据 | 每张图表可下钻到明细/证据（洞察 evidence），保持"洞察可执行"家族基因 |
| 数据可溯 | 图表标注数据窗口与来源（GSC/GA4/GDELT…），避免"黑盒数字" |

---

## 2. 图表语言规范（什么数据用什么图）

| 数据形态 | 推荐图形 | 禁用/避免 |
|---|---|---|
| 单值 + 环比 | **KPI 卡**（大数字 + 环比箭头 + 迷你 sparkline） | 用饼图表示单值 |
| 时序趋势（连续） | **折线/面积图**（多序列对比用折线） | 折线画分类数据 |
| 分类对比（离散） | **横向条形图**（名称长时优于柱状） | 3D 柱 |
| 构成占比（≤5 类） | **堆叠条/百分比条**（优于饼图） | 饼图 >6 类 |
| 情绪/分布 | **堆叠百分比条 + 计数徽标** | 饼图 |
| 漏斗/旅程步 | **漏斗图**（含相邻步转化率标注） | 折线替代 |
| 多维评分（5 维） | **雷达图**（成熟度） | 表格硬看 |
| 两维机会矩阵 | **散点四象限**（搜索量 × 竞争力） | 单列排序 |
| 双维交叉（时段×渠道） | **热力矩阵** | 堆叠柱 |
| 进度/配额 | **进度条 / 环形 Gauge** | 纯数字 |
| 事件与异动 | **时间线（Timeline）** | 表格平铺 |
| 明细与检索 | **表格（排序 + 分页 + 状态徽标）** | 图表代替明细 |
| 关键词/话题热度 | **标签条云（带权重字号）** | 纯词云（不可读） |

---

## 3. 驾驶舱清单（9 个）

### C1 · 情报总览驾驶舱（Overview）★首页
**回答**：我现在最该关注什么？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 全域健康 KPI | 新增洞察数 / 待处理 / 负面预警数 / 采集成功率 / 验证有效数 | 5 张 KPI 卡（含环比 sparkline） |
| 今日情报流 | 洞察（severity 排序） | 时间线 + 严重度徽标，可点击下钻 |
| 风险雷达 | 各监测主体负向占比 | 横向条形图（阈值线标注） |
| 闭环进度 | 洞察状态分布（new→ack→actioned→verified） | 堆叠百分比条 |
| 监测覆盖 | 5 类监控任务数与最近运行状态 | 状态徽标小卡 + 迷你时间线 |
| 快捷动作 | 触发诊断 / 生成周报 / 新建监测 | 按钮组 |

**筛选**：时间范围（24h/7d/30d/90d/自定义）、监测主体、严重度、渠道

### C2 · 舆情驾驶舱（Sentiment）★已有雏形
**回答**：我的品牌/话题在网上被怎么说？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 舆情 KPI | 总提及 / 负面数 / 平均负向占比 / 负面预警数 | KPI 卡 |
| 情绪趋势 | negative_ratio + avg_score 时序 | 双轴折线（占比左轴 / 分数右轴） |
| 渠道构成 | topic_mentions_{news,search,reddit} | 堆叠面积图 |
| 主体热力 | 各主体负向占比 | 横向条形 + 阈值线（35%） |
| 负面预警 | topic_negative_alert | 卡片列表（负面样本 + 命中词高亮） |
| 高频负向词 | 负面命中词频次 | 标签条云（字号=频次） |
| 提及时间线 | 全部提及（标题+来源） | 时间线（按小时分桶） |

**筛选**：主体（多选）、渠道、情绪标签、时间范围、来源域名、关键词

### C3 · 流量驾驶舱（Traffic）
**回答**：流量/转化为什么波动？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 流量 KPI | 点击/曝光/CTR/平均排名/会话/转化 | KPI 卡（每项 7d 环比） |
| 流量趋势 | gsc_clicks、ga4_sessions | 多序列折线（可切换指标） |
| 渠道分解 | Organic/Paid/Referral/Social/Direct/AI | 堆叠柱（按周） |
| 关键词机会 | 曝光 × 排名，CTR 缺口 | 散点四象限（右上=机会） |
| 着陆页表现 | Top 页点击/转化 | 表格（排序 + 迷你条） |
| 技术健康 | CrUX LCP/INP/CLS/TTFB | Gauge 组（good/needs/poor 三色） |
| 异常标注 | traffic_anomaly / conversion_low | 折线图上标注点 |

**筛选**：时间范围、来源渠道、设备（CrUX）、页面/关键词、竞品对比开关

### C4 · 竞品驾驶舱（Competitor）
**回答**：对手在做什么、我差在哪？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 竞品 KPI | 监控竞品数 / 本周期异动数 / 关键词缺口数 | KPI 卡 |
| 定价监控 | 各竞品定价档位 + 变动 | 阶梯条形 + 变动徽标 |
| 关键词缺口 | 竞品有排名、我方缺失 | 表格（搜索量排序 + 机会分）+ 条形 |
| 排名追踪 | 关键词排名变化 | 折线（多关键词，选中高亮） |
| 异动时间线 | site_change / competitor_pricing | 时间线（diff 摘要） |
| 广告素材 | ad_creatives | 卡片网格（标题/落地页/首发时间） |
| 竞品档案 | profiles + seo 快照 | 卡片（定位/定价/社媒） |

**筛选**：竞品（多选）、监控类型（定价/内容/SEO/广告）、时间范围、关键词

### C5 · 客户旅程驾驶舱（Journey & RFM）
**回答**：用户在哪一步流失、谁值得深耕？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 漏斗 | journey_step 各步人数 | 漏斗图（标注相邻步转化率） |
| 断点清单 | journey_gap | 表格 + 严重度徽标 |
| 覆盖热力 | 旅程框架 × 触点数 | 热力矩阵（空心阶段标红） |
| RFM 分群 | 6 分群人数与价值 | 气泡图（人数 × 客单）或堆叠条 |
| 流失预警 | rfm_at_risk | 卡片（人数/价值/建议动作） |
| 留存趋势 | ga4_retention | 折线（含基准线） |

**筛选**：旅程框架（STDC/AIDA/自定义）、时间范围、分群、来源渠道

### C6 · 行动与验证驾驶舱（Action Loop）★北极星
**回答**：建议的动作到底有没有用？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 闭环 KPI | 派发/完成/验证中/已验证 | KPI 卡 + 环形 Gauge |
| 验证结论构成 | effective/neutral/harmful | 堆叠百分比条 |
| 模型命中率 | get_model_effectiveness | 横向条形（排序） |
| 验证报告列表 | verification 报告 | 表格（结论徽标 + 前后对比） |
| 动作时间线 | action.dispatched→verified | 时间线（状态机可视化） |
| 待评估 | 到期未验证动作 | 警示条 + 列表 |

**筛选**：时间范围、动作类型（mflow/openflow/webhook）、模型、结论

### C7 · 报告中心驾驶舱（Reports）
**回答**：我要给客户/自己交付什么？
| 看板 | 数据 | 呈现 |
|---|---|---|
| 报告目录 | weekly/diagnosis/competitors/maturity/verification/deep-dive | 卡片网格（带摘要缩略图） |
| 可视化报告 | **Markdown → 可视 HTML**（内联 SVG 图表） | 分段式报告（KPI+图表+表格） |
| 白标导出 | 品牌化 HTML / 打印 PDF | 导出按钮（Scale 档） |
| 报告订阅 | 报告生成 → 订阅推送 | 开关 + 渠道徽标 |

**筛选**：报告类型、时间范围、客户（白标）、工作区

### C8 · 运行监控驾驶舱（Ops）
**回答**：系统还健康吗？（面向运维/私有化）
| 看板 | 数据 | 呈现 |
|---|---|---|
| 采集成功率 | monitor.run_finished / alert | KPI + 折线 |
| 任务状态 | 5 类监控运行状态 | 状态网格（绿/黄/红） |
| 配额消耗 | QuotaLedger 用量/限额 | 进度条组 + 熔断状态 |
| 告警流 | 分级告警事件 | 时间线（critical 红） |
| 订阅推送 | subscription.pushed / failed | KPI + 失败率折线 |
| 备份状态 | backup.completed | 状态卡（最近备份时间/大小） |
| 授权健康 | token expiry_status | 徽标（剩余天数） |

**筛选**：时间范围、事件级别、数据源、监控类型

### C9 · 商业化驾驶舱（Billing）
**回答**：我的用量与成本是否健康？（也用于云托管运营）
| 看板 | 数据 | 呈现 |
|---|---|---|
| 套餐/试用 | plan + trial_status | 卡（剩余天数进度条） |
| 用量 KPI | api_calls/agent_asks/deep_reports/cost | KPI + 配额进度条 |
| 用量趋势 | 每日用量 | 折线（按类型堆叠） |
| 数据源成本 | 各 source 花费 | 横向条形（按费用排序） |
| 对账单 | monthly invoice | 表格 + 导出 |

**筛选**：时间范围（当月/上月/自定义）、用量类型、数据源

---

## 4. 筛选器体系

**全局筛选（驾驶舱级，右上角）**
- 时间范围：24h / 7d / 30d / 90d / 自定义
- 工作区/客户：多租户/代理场景切换
- 监测主体：话题/品牌/竞品（多选）
- 渠道：news / search / reddit / gsc / ga4 / crux

**局部筛选（看板级）**
- 严重度（critical→info）、状态（new→verified）、洞察类型前缀
- 来源域名、关键词、分群、旅程阶段（S0–S3）
- 置信度下限

**交互约定**
- 筛选即 URL 参数（可分享、可收藏）
- 图表点击 = 下钻（图表 → 明细表 → 洞察证据）
- 时间范围与所有看板联动（一次改，全屏变）

---

## 5. 报告可视化升级（MD → 可视 HTML）

| 项 | 现状 | 目标 |
|---|---|---|
| 格式 | Markdown | **自包含 HTML**（内联 SVG，可打印 PDF） |
| 图表 | 无 | 按报告类型嵌入对应看板图表（周报=趋势+Top；深度=全量看板） |
| 导出 | 复制 MD | 打印 PDF / 白标 HTML / 分享链接 |
| 数据溯源 | 文本 | 每个图表标注窗口+来源，可点击回看洞察 |

报告结构模板（深度报告）：
`执行摘要(KPI) → 流量(折线+散点) → 竞品(条形+时间线) → 舆情(堆叠条+词云) → 旅程(漏斗) → 行动(命中率) → 90 天路线`

---

## 6. 技术实现方案

**新增模块**
```
insflow/viz/                 # 服务端 SVG 图表（零依赖）
├── primitives.py           # 坐标轴/刻度/网格/图例/颜色令牌
├── charts.py               # kpi / line / bar / stacked / funnel / heatmap /
│                           # scatter / radar / gauge / timeline / tagcloud / table
└── theme.py                # 明暗主题色板（与 base.html CSS 变量同源）
insflow/web/cockpit/         # 驾驶舱数据聚合（每舱一个 aggregator）
└── aggregators.py          # 从 metrics/insights/actions/events 聚合出看板数据
```

**数据面**：复用现有表（metrics/insights/actions/feedback/events/subscriptions），
补充聚合 API：`GET /api/v1/cockpit/{name}?workspace_id=&range=&filters=`

**渲染路径**：驾驶舱页面 = 控制台路由（Jinja2）+ SVG 组件（服务端生成），
图表数据同时以 JSON 暴露，便于将来换前端图表库或导出。

**报告**：`ReportRenderer` 把报告 Markdown 的图表占位符（如 `:::chart traffic_trend:::`）
替换为对应看板的 SVG，产出可视 HTML。

---

## 7. 实施优先级

| 优先级 | 内容 | 状态 |
|---|---|---|
| **P0** | viz 图表基座（13 类图表）+ 报告可视 HTML | ✅ 已完成 |
| **P0** | 9 个驾驶舱全部落地（C1–C9） | ✅ 已完成 |
| **P0** | **交互层**：hover 提示 + 折线十字准线 + 图表下钻抽屉 + 图/表切换 + 单图 CSV 导出 + 全局筛选栏（时间/主体/渠道，URL 可分享） | ✅ 已完成 |
| **P1** | 同期对比虚线 · 事件标注钉在时间轴 · 图例开关 · 时间序列缩放/平移 · 无障碍表格回退 | ⏳ 未做 |
| **P2** | Canvas 大数据量 · 桑基/地图/箱线 · brushing 联动 · 实时流 · 嵌入 embed token · 看板自定义布局 | ⏳ 未做 |

---

## 7.1 能力现状与诚实边界（2026-09-17 复核）

**已具备**：13 类图表 · 9 舱 · 服务端 SVG（零依赖/可打印/白标）· TTL 缓存与降采样 ·
hover 提示与十字准线 · 图表下钻（时间序列 + 关联洞察）· 图/表切换 · 单图 CSV ·
全局筛选（时间/主体/渠道，URL 可分享）。

**尚未具备（如实标注）**：
- 交互深度：无框选/缩放平移/brushing 联动/图例开关/动画/十字准线仅折线
- 数据规模：SVG 渲染上限（>1-2k 节点劣化），单序列降采样上限 120 点；无 Canvas/WebGL
- 探索能力：只有固定看板，**无即席探索**（无拖拽、无指标目录、无语义层）
- 分析能力：无同环比虚线、无预测区间、无注释层、无图表级订阅
- 可访问性：无屏幕阅读器支持/表格回退以外的 a11y 处理
- 交付形态：无嵌入（iframe/embed token）、无看板自定义布局、无 PNG/Excel 导出

**可比对象定位**：交互与交付能力约等于 **Evidence.dev / Observable 这类代码化报表**；
**远不及** Tableau / Power BI / Metabase / Superset / Grafana / ECharts 的深度与探索能力。
差异化优势不在可视化本身，而在**图表服务于「洞察 → 动作 → 14 天验证」闭环**，
以及**自包含可交付报告（打印 PDF / 白标脱敏 / 私有化零依赖）**。

---

## 8. 验收标准

- 任一驾驶舱：**一屏内**给出该主题的"结论 + 证据 + 下一步动作"
- 所有图表：明暗主题正常、可打印、无外部 CDN 依赖
- 报告：从 Markdown 升级为可视 HTML，含 ≥3 类图表，可导出 PDF
- 筛选：全局时间范围改动后，全屏看板同步刷新（URL 可分享）
